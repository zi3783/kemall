<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kemall.product.mapper.ProductMapper">

    <resultMap id="productDetail" type="com.kemall.product.domain.vo.ProductVO">
        <id column="id" property="id"/>
        <result column="product_code" property="productCode"/>
        <result column="name" property="name"/>
        <result column="main_image" property="mainImage"/>
        <result column="description" property="description"/>
        <result column="category_id" property="categoryId"/>
        <result column="brand_id" property="brandId"/>
        <result column="price" property="price"/>
        <result column="brand_name" property="brandName"/>
        <result column="category_name" property="categoryName"/>
        <collection property="productSkuVOList" ofType="com.kemall.product.domain.vo.ProductSkuVO">
            <id column="sku_id" property="id"/>
            <result column="sku_code" property="skuCode"/>
            <result column="sku_price" property="price"/>
            <result column="sku_image" property="mainImage"/>
            <result column="sku_json" property="specJson"/>
            <result column="product_id" property="productId"/>
        </collection>
    </resultMap>
    <select id="queryProductDetailById" resultMap="productDetail">
        select product.*,
        brand.name as brand_name,
        category.name as category_name,
        product_sku.price as sku_price,
        product_sku.main_image as sku_image,
        product_sku.spec_json as sku_json,
        product_sku.id as sku_id,
        product_sku.sku_code as sku_code,
        product_sku.product_id as product_id
        from product
        left join brand on product.brand_id = brand.id
        left JOIN category on product.category_id = category.id
        left join product_sku on product.id = product_sku.product_id and product_sku.status = 1
        where product.id = #{productId}
        and product.status = 4
    </select>



    <select id="queryProductIntroByCondition" resultType="com.kemall.product.domain.vo.ProductIntro">
        select product.id, product_code, product.name, main_image, price
        from product
        join category on product.category_id = category.id
        WHERE category.path LIKE (
        select CONCAT(path, '/%')
        from category
        where id = #{categoryId}
        )
        and product.status = 4
        and category.status = 1
        <where>
            <if test="brandId != null">
                and brand_id = #{brandId}
            </if>
        </where>
    </select>
</mapper>
